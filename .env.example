# ElevenLabs API key (required)
# Used at runtime for radio bait TTS (injected into client bundle via Vite)
# Used by scripts/generate-audio.ts for audio asset generation
# Get a key at https://elevenlabs.io
ELEVENLABS_API_KEY=sk-your-key-here
