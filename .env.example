# ElevenLabs API key (required)
# Used server-side by the Vercel TTS proxy (api/tts.ts) for radio bait
# Used by scripts/generate-audio.ts for offline audio asset generation
# Get a key at https://elevenlabs.io
ELEVENLABS_API_KEY=sk-your-key-here
